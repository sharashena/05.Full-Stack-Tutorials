// import { useParams, Link } from "react-router-dom";

// with useParams we can access object with key/value pairs what we passed when we set Routes

// const SingleProduct = () => {
//   const { productId } = useParams();
//   return (
//     <div>
//       <h2>{productId}</h2>
//       <Link to="/products">back to products</Link>
//     </div>
//   );
// };

// export default SingleProduct;
